<%= simple_form_for @blog do |form| %>
  <% if blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= form.file_field :image %>
  </div>

  <div class="field">
    <%= form.input :title, id: :blog_title %>
  </div>
  
  <div class="field">
    <%= form.input :description, id: :blog_description %>
  </div>
  
  <div class="field">
    <%= form.input :content, as: :summernote, id: :blog_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
